const initialState = {
  shoppingListItems: [{text: 'x', className: 'unchecked'}, 
                      {text: 'y', className: 'unchecked'}, 
                      {text: 'z', className: 'unchecked'}],
  
};

const reduce = (state = initialState, action) => {
  return state;
};

export default reduce;
